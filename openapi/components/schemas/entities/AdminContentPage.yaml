type: object
additionalProperties: false
required: [data, page]
description: |
  Cursor page of hideable content for the back-office (FS-ADM-03). Newest
  first. `page.next` is opaque; pass it as `after` for the next page.
properties:
  data:
    type: array
    items:
      $ref: ./AdminContent.yaml
  page:
    $ref: ./Page.yaml

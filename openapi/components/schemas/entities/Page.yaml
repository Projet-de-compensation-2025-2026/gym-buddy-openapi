type: object
additionalProperties: false
required: [size]
description: Cursor page metadata. `next` is opaque; null means the last page.
properties:
  next:
    type: [string, "null"]
  size:
    type: integer
    minimum: 1
    maximum: 50

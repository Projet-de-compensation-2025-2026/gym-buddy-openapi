type: object
additionalProperties: false
required: [userId, handle, displayName]
description: The other member on a friendship row, relative to the caller.
properties:
  userId:
    type: string
    format: uuid
  handle:
    type: string
    minLength: 1
  displayName:
    type: string
  sports:
    type: array
    maxItems: 12
    items:
      type: string
      minLength: 2
      maxLength: 32
